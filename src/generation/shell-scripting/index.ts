/**
 * Shell Scripting Utilities for flowsh Workflows
 *
 * Contains shell script generation functions for headers, utilities, mocks, and footers.
 * These are the core shell functions that support workflow execution.
 */

import { GenerationOptions } from '../shell-generator.js';
import { FlowshWorkflow } from '../../dsl/types.js';

/**
 * Shell script utilities generator
 */
export class ShellScriptingUtils {
  /**
   * Generates the shell script header with shebang, metadata, and initial setup
   */
  static generateScriptHeader(workflow: FlowshWorkflow, options: GenerationOptions): string {
    const workflowName = workflow.workflow?.name || workflow.metadata?.name || 'Unnamed Workflow';
    const workflowVersion = workflow.workflow?.version || workflow.metadata?.version || '1.0.0';
    const description = workflow.workflow?.description || workflow.metadata?.description || '';

    return `#!/bin/${options.shell || 'bash'}
# Generated by flowsh v1.0.0
# Source: ${workflowName}
${description ? `# Description: ${description}` : ''}
# Version: ${workflowVersion}
# Generated: ${new Date().toISOString()}

set -euo pipefail

# =============================================================================
# WORKFLOW CONFIGURATION
# =============================================================================

# Workflow variables
declare -A workflow_vars
declare -A workflow_state

# Control flags for testing and configuration
USE_MOCK_TOOLS=\${USE_MOCK_TOOLS:-true}
AGENT_TIMEOUT=\${AGENT_TIMEOUT:-${options.defaultTimeout || 60}}
VERBOSE=\${VERBOSE:-${options.verbose ? 'true' : 'false'}}
WORKFLOW_NAME="${workflowName}"
WORKFLOW_VERSION="${workflowVersion}"

# Color codes for output
RED='\\033[0;31m'
GREEN='\\033[0;32m'
YELLOW='\\033[1;33m'
BLUE='\\033[0;34m'
CYAN='\\033[0;36m'
NC='\\033[0m' # No Color`;
  }

  /**
   * Generates core utility functions for logging, error handling, and variable management
   */
  static generateUtilityFunctions(): string {
    return `# =============================================================================
# UTILITY FUNCTIONS
# =============================================================================

# Logging functions with colors
log_info() {
    echo -e "\${CYAN}ℹ️  \$*\${NC}"
}

log_success() {
    echo -e "\${GREEN}✅ \$*\${NC}"
}

log_error() {
    echo -e "\${RED}❌ \$*\${NC}" >&2
}

log_warning() {
    echo -e "\${YELLOW}⚠️  \$*\${NC}" >&2
}

log_debug() {
    [[ "\$VERBOSE" == "true" ]] && echo -e "\${BLUE}🔍 DEBUG: \$*\${NC}" >&2 || true
}

log_step() {
    echo -e "\${CYAN}🚀 \$*\${NC}"
}

# Operation logging with correlation IDs
log_operation_start() {
    local operation_name="\$1"
    local operation_type="\$2"
    local correlation_id="\$3"
    log_debug "\$correlation_id" "Starting \$operation_type: \$operation_name"
}

log_operation_end() {
    local operation_name="\$1"
    local operation_type="\$2"
    local correlation_id="\$3"
    local status="\${4:-success}"
    log_debug "\$correlation_id" "Completed \$operation_type: \$operation_name (\$status)"
}

# Enhanced error handling
handle_error() {
    local exit_code=\$1
    local context="\$2"
    local node_id="\${3:-unknown}"
    
    case \$exit_code in
        124)
            log_error "Timeout: \$context took longer than \${AGENT_TIMEOUT}s (node: \$node_id)"
            ;;
        127)
            log_error "Command not found in: \$context (node: \$node_id)"
            ;;
        1)
            log_error "General error in: \$context (node: \$node_id)"
            ;;
        *)
            log_error "Unknown error (exit code \$exit_code) in: \$context (node: \$node_id)"
            ;;
    esac
    
    return \$exit_code
}

# Variable management
set_workflow_var() {
    local var_name="\$1"
    local var_value="\$2"
    workflow_vars["\$var_name"]="\$var_value"
    log_debug "Set variable: \$var_name = \$var_value"
}

get_workflow_var() {
    local var_name="\$1"
    local default_value="\${2:-}"
    echo "\${workflow_vars[\$var_name]:-\$default_value}"
}

# State management
set_workflow_state() {
    local state_key="\$1"
    local state_value="\$2"
    workflow_state["\$state_key"]="\$state_value"
    log_debug "Set state: \$state_key = \$state_value"
}

get_workflow_state() {
    local state_key="\$1"
    local default_value="\${2:-}"
    echo "\${workflow_state[\$state_key]:-\$default_value}"
}

# Variable substitution in templates
substitute_variables() {
    local template="\$1"
    local result="\$template"
    
    # Find all variable references in the format {{#variable_name#}}
    while [[ \$result =~ \\{\\{#([^#]+)#\\}\\} ]]; do
        local var_ref="\${BASH_REMATCH[1]}"
        local var_value=\$(get_workflow_var "\$var_ref" "")
        
        if [[ -z "\$var_value" ]]; then
            log_warning "Variable reference '\$var_ref' not found, using empty string"
        fi
        
        # Escape special characters for sed
        local escaped_value=\$(printf '%s\\n' "\$var_value" | sed 's/[[\\.\\*^\\$()+?{|]/\\\\&/g')
        result=\$(echo "\$result" | sed "s|{{#\${var_ref}#}}|\$escaped_value|g")
    done
    
    echo "\$result"
}`;
  }

  /**
   * Generates mock functions for testing workflow execution without real tools
   */
  static generateMockFunctions(): string {
    return `# =============================================================================
# MOCK FUNCTIONS FOR TESTING
# =============================================================================

# Mock opencode command for testing
mock_opencode() {
    local prompt="\$1"
    log_info "Mock opencode called with prompt:"
    echo "---"
    echo "\${prompt:0:200}..." # Show first 200 chars
    echo "---"
    
    # Simulate some processing time
    sleep 2
    
    log_success "Mock agent completed task successfully"
    return 0
}

# Mock LLM call for testing  
mock_llm() {
    local prompt="\$1"
    local model="\${2:-gpt-4}"
    
    log_info "Mock LLM (\$model) called with prompt:"
    echo "---"
    echo "\${prompt:0:200}..." # Show first 200 chars
    echo "---"
    
    # Simulate processing time based on model
    case "\$model" in
        *"gpt-4"*) sleep 3 ;;
        *"claude"*) sleep 2 ;;
        *) sleep 1 ;;
    esac
    
    log_success "Mock LLM response generated"
    echo "This is a mock response from \$model. In a real implementation, this would be the actual LLM output."
    return 0
}

# Mock command execution
mock_command() {
    local command="\$1"
    shift
    local args=("\$@")
    
    log_info "Mock command: \$command \${args[*]}"
    
    case "\$command" in
        "make")
            case "\${args[0]:-}" in
                "test")
                    log_info "Running tests..."
                    sleep 1
                    log_success "All tests passed"
                    return 0
                    ;;
                "build")
                    log_info "Building project..."
                    sleep 2
                    log_success "Build completed"
                    return 0
                    ;;
                *)
                    log_error "Unknown make target: \${args[0]:-}"
                    return 1
                    ;;
            esac
            ;;
        "npm"|"yarn"|"pnpm")
            log_info "Running package manager command..."
            sleep 1
            log_success "Package manager command completed"
            return 0
            ;;
        "git")
            log_info "Running git command..."
            sleep 1
            log_success "Git command completed"
            return 0
            ;;
        *)
            log_info "Running generic command: \$command"
            sleep 1
            log_success "Command completed"
            return 0
            ;;
    esac
}`;
  }

  /**
   * Generates the main execution flow and script footer
   */
  static generateScriptFooter(): string {
    return `# =============================================================================
# MAIN EXECUTION
# =============================================================================

# Health check function
health_check() {
    log_info "Running system health check..."
    
    # Check required commands based on mode
    if [[ "\$USE_MOCK_TOOLS" != "true" ]]; then
        local missing_tools=()
        
        command -v opencode >/dev/null 2>&1 || missing_tools+=("opencode")
        
        if [[ \${#missing_tools[@]} -gt 0 ]]; then
            log_info "Missing tools (will use mocks): \${missing_tools[*]}"
        else
            log_success "All required tools available"
        fi
    else
        log_info "Running in mock mode - no external tools required"
    fi
}

# Main function
main() {
    health_check
    init_workflow "\$@"
    execute_workflow
}

# Run if called directly
if [[ "\${BASH_SOURCE[0]}" == "\${0}" ]]; then
    main "\$@"
fi`;
  }

  /**
   * Generates workflow-specific argument parsing
   */
  static generateArgumentParsing(workflow: FlowshWorkflow): string {
    const envVars = workflow.environment_variables || workflow.spec?.environment_variables || [];
    const convVars = workflow.conversation_variables || workflow.spec?.conversation_variables || [];

    // Combine all variables
    const allVariables = [...envVars, ...convVars];

    let parseLogic = '';
    let helpEchos: string[] = [];

    allVariables.forEach((variable: any) => {
      const varName = variable.variable;
      const description = variable.description || variable.name || '';

      // Add to help text as echo statements
      helpEchos.push(`    echo "    --${varName}"`);
      if (description) {
        helpEchos.push(`    echo "        ${description}"`);
      }

      // Add parsing logic
      parseLogic += `        --${varName})
            if [[ -z "\${2:-}" ]]; then
                log_error "Option --${varName} requires a value"
                show_help
                exit 1
            fi
            set_workflow_var "${varName}" "\$2"
            shift 2
            ;;
`;
    });

    return `# =============================================================================
# WORKFLOW INITIALIZATION  
# =============================================================================

# Show help message
show_help() {
    echo "Usage: \$0 [OPTIONS]"
    echo ""
    echo "Generated workflow script for: \${WORKFLOW_NAME}"
    echo ""
    echo "Options:"
${helpEchos.join('\n')}
    echo "    --help, -h      Show this help message"
    echo "    --mock         Use mock tools (default: true)"  
    echo "    --real         Use real tools instead of mocks"
    echo "    --verbose      Enable verbose output"
    echo "    --timeout=N    Set timeout in seconds (default: \${AGENT_TIMEOUT})"
    echo ""
    echo "Environment Variables:"
    echo "    USE_MOCK_TOOLS    Set to 'false' to use real tools"
    echo "    VERBOSE          Set to 'true' for debug output"
    echo "    AGENT_TIMEOUT    Timeout in seconds for agent calls"
}

# Initialize workflow with command line arguments
init_workflow() {
    log_info "Initializing workflow: \${WORKFLOW_NAME} v\${WORKFLOW_VERSION}"
    
    # Parse command line arguments
    while [[ \$# -gt 0 ]]; do
        case \$1 in
${parseLogic}        --help|-h)
            show_help
            exit 0
            ;;
        --mock)
            USE_MOCK_TOOLS=true
            export USE_MOCK_TOOLS
            shift
            ;;
        --real)
            USE_MOCK_TOOLS=false
            export USE_MOCK_TOOLS
            shift
            ;;
        --verbose)
            VERBOSE=true
            export VERBOSE
            shift
            ;;
        --timeout=*)
            AGENT_TIMEOUT="\${1#*=}"
            export AGENT_TIMEOUT
            shift
            ;;
        *)
            log_error "Unknown option: \$1"
            show_help
            exit 1
            ;;
        esac
    done
    
    # Set default values for missing variables
    ${allVariables
      .map((variable: any) => {
        const defaultValue = variable.default || '';
        return `    [[ -z "\$(get_workflow_var '${variable.variable}' '')" ]] && set_workflow_var "${variable.variable}" "${defaultValue}"`;
      })
      .join('\n    ')}
    
    log_info "Workflow initialization completed"
}`;
  }

  /**
   * Generates the main workflow flow execution
   */
  static generateMainFlow(nodes: any[], edges: any[]): string {
    // Build adjacency list for workflow execution order
    const adjacencyList = new Map<string, string[]>();
    const inDegree = new Map<string, number>();

    // Initialize all nodes
    nodes.forEach(node => {
      adjacencyList.set(node.id, []);
      inDegree.set(node.id, 0);
    });

    // Build the graph from edges
    edges.forEach(edge => {
      const source = edge.source;
      const target = edge.target;

      if (adjacencyList.has(source) && inDegree.has(target)) {
        adjacencyList.get(source)!.push(target);
        inDegree.set(target, (inDegree.get(target) || 0) + 1);
      }
    });

    // Find start node(s) - nodes with zero in-degree
    const startNodes = Array.from(inDegree.entries())
      .filter(([_, degree]) => degree === 0)
      .map(([nodeId, _]) => nodeId);

    // Generate execution logic
    let flowLogic = '';

    if (startNodes.length === 0) {
      // Fallback: find explicit start node
      const startNode = nodes.find(n => n.type === 'start');
      if (startNode) {
        flowLogic = `    # Execute start node
    execute_node_${startNode.id.replace(/[^a-zA-Z0-9]/g, '_')}
    
    # Execute remaining nodes in sequence (fallback mode)
    ${nodes
      .filter(n => n.type !== 'start')
      .map(n => `execute_node_${n.id.replace(/[^a-zA-Z0-9]/g, '_')}`)
      .join('\n    ')}`;
      } else {
        flowLogic = '    log_error "No start node found in workflow"';
      }
    } else {
      // Generate proper topological execution order
      flowLogic = this.generateTopologicalExecution(nodes, adjacencyList, startNodes);
    }

    return `# =============================================================================
# WORKFLOW EXECUTION FLOW
# =============================================================================

# Execute the complete workflow
execute_workflow() {
    log_info "Starting workflow execution..."
    set_workflow_state "status" "running"
    
    # Generate unique correlation ID for this execution
    local correlation_id=\$(date +%s%N | sha256sum | head -c 8)
    
${flowLogic}
    
    log_success "Workflow execution completed"
    set_workflow_state "status" "completed"
}`;
  }

  /**
   * Generates topological execution order for workflow nodes
   */
  private static generateTopologicalExecution(
    nodes: any[],
    adjacencyList: Map<string, string[]>,
    startNodes: string[]
  ): string {
    const executionSteps: string[] = [];

    // Simple approach: execute start nodes, then follow edges
    startNodes.forEach(nodeId => {
      const node = nodes.find(n => n.id === nodeId);
      if (node) {
        executionSteps.push(`    # Execute ${node.type} node: ${nodeId}`);
        executionSteps.push(
          `    if ! execute_node_${nodeId.replace(/[^a-zA-Z0-9]/g, '_')} "" "\$correlation_id"; then`
        );
        executionSteps.push(`        log_error "Workflow failed at node: ${nodeId}"`);
        executionSteps.push(`        set_workflow_state "status" "failed"`);
        executionSteps.push(`        return 1`);
        executionSteps.push(`    fi`);
        executionSteps.push('');

        // Add subsequent nodes connected to this node
        const connectedNodes = this.getConnectedExecutionChain(nodeId, adjacencyList);
        connectedNodes.forEach(nextNodeId => {
          const nextNode = nodes.find(n => n.id === nextNodeId);
          if (nextNode) {
            executionSteps.push(`    # Execute ${nextNode.type} node: ${nextNodeId}`);
            executionSteps.push(
              `    if ! execute_node_${nextNodeId.replace(/[^a-zA-Z0-9]/g, '_')} "" "\$correlation_id"; then`
            );
            executionSteps.push(`        log_error "Workflow failed at node: ${nextNodeId}"`);
            executionSteps.push(`        set_workflow_state "status" "failed"`);
            executionSteps.push(`        return 1`);
            executionSteps.push(`    fi`);
            executionSteps.push('');
          }
        });
      }
    });

    return executionSteps.join('\n');
  }

  /**
   * Gets the chain of connected nodes from a starting node
   */
  private static getConnectedExecutionChain(
    startNodeId: string,
    adjacencyList: Map<string, string[]>
  ): string[] {
    const visited = new Set<string>();
    const executionChain: string[] = [];

    const traverse = (nodeId: string) => {
      if (visited.has(nodeId)) return;

      visited.add(nodeId);
      const connected = adjacencyList.get(nodeId) || [];

      connected.forEach(nextNodeId => {
        if (!visited.has(nextNodeId)) {
          executionChain.push(nextNodeId);
          traverse(nextNodeId);
        }
      });
    };

    traverse(startNodeId);
    return executionChain;
  }
}
